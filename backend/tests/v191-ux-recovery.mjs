import assert from 'node:assert/strict';
import {readFileSync} from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');
const read=p=>readFileSync(path.join(root,p),'utf8');
const ux=read('frontend/js/ux-v191.js');
const css=read('frontend/experience-v191.css');
const enhancements=read('frontend/js/enhancements-entry.js');
const auth=read('frontend/js/auth-entry.js');
assert.match(ux,/Opérations/,'visible Operations hub missing');
assert.match(ux,/DÉVELOPPEMENT RÉSEAU/,'enriched Development workspace missing');
assert.match(ux,/GO/,'Development decision state missing');
assert.match(ux,/CAPEX/,'Development business case missing');
assert.match(ux,/simplifyDirectorNavigation/,'director navigation simplification missing');
assert.match(ux,/setPage\(b\.dataset\.ux191Go\)/,'operations must navigate to canonical StoreOps pages');
assert.match(css,/ux191-board/,'V1.91 responsive project board styles missing');
assert.match(enhancements,/\.\/ux-v191\.js/,'V1.91 UX layer must be deferred with enhancements');
assert.match(enhancements,/v=1910/,'enhancement cache version must change');
assert.match(auth,/BUILD='1910'/,'frontend runtime must invalidate the prior cached graph');
console.log('V1.91 visible UX recovery contract OK');
