import{api}from'../api.js';import{app,canManage}from'../state.js';import{$,status,fmtDate,esc,toast}from'../ui.js';export async function renderDlc(){const rows=await api(`/api/stores/${app.storeId}/dlc`);$('#dlcContent').innerHTML=`${canManage()?`<div class="card"><strong>Ajouter une DLC</strong><div class="form-grid" style="margin-top:10px"><div class="field"><label>EAN</label><input id="dlcEan" placeholder="Scanner / saisir"></div><div class="field"><label>DLC</label><input id="dlcDate" type="date"></div><div class="field"><label>Quantité</label><input id="dlcQty" type="number" min="0"></div><div class="field"><label>Zone</label><select id="dlcZone"><option>Rayon</option><option>Réserve</option><option>Chambre froide</option><option>Fruits & légumes</option></select></div><div class="field"><label>Lot / repère</label><input id="dlcLot"></div><div class="field"><label>Commentaire</label><input id="dlcComment"></div></div><button class="btn brand" id="saveDlc" style="margin-top:11px">Enregistrer la DLC</button></div>`:`<div class="role-lock">Lecture seule.</div>`}<div class="card" style="margin-top:14px"><div class="row"><strong>Registre DLC</strong><span class="pill">${rows.length} ligne(s)</span></div><div class="table-wrap"><table class="table"><thead><tr><th>Article</th><th>DLC</th><th>Qté</th><th>Zone</th><th>Risque</th><th>Auteur</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${esc(r.product_name)}</strong><div class="small muted">${esc(r.ean)}</div></td><td>${fmtDate(r.expiry_date)}</td><td>${r.quantity}</td><td>${esc(r.zone)}</td><td>${status(r.risk.label,r.risk.severity==='CRITICAL'?'danger':['HIGH','MEDIUM'].includes(r.risk.severity)?'warn':'ok')}</td><td>${esc(r.created_by_name)}</td></tr>`).join('')||'<tr><td colspan="6"><div class="empty">Aucune DLC.</div></td></tr>'}</tbody></table></div></div>`;$('#saveDlc')?.addEventListener('click',save)}async function save(){const b={ean:$('#dlcEan').value.trim(),expiryDate:$('#dlcDate').value,quantity:Number($('#dlcQty').value),zone:$('#dlcZone').value,lotRef:$('#dlcLot').value.trim(),comment:$('#dlcComment').value.trim()};await api(`/api/stores/${app.storeId}/dlc`,{method:'POST',body:JSON.stringify(b)});toast('DLC enregistrée.');renderDlc()}