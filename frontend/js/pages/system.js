import{api}from'../api.js';import{app,isDirector}from'../state.js';import{$,status,esc}from'../ui.js';import{integrationReadiness,readinessCounts}from'../integration-readiness.js';
const check=(label,x)=>`<div class="focus-meta"><span>${esc(label)}</span>${status(x?.ok?'OK':x?.skipped?'À faire':'NOK',x?.ok?'ok':x?.skipped?'neutral':'danger')}</div>${x?.error?`<div class="small muted" style="margin:4px 0 10px">${esc(x.error)}</div>`:''}`;
const showcaseDiagnostics=()=>({connected:false,mode:'SIMULATED',expectedAudience:null,configuration:{baseUrl:{configured:false,value:null},tenantId:{configured:false,value:null},clientId:{configured:false,value:null},clientSecret:{configured:false},oauthVersion:'v2',readModes:{product:'simulated',stock:'simulated',price:'simulated',promotion:'simulated',receiving:'simulated',assortment:'simulated',taxonomy:'simulated',sales:'simulated'},mappings:{}},checks:{config:{ok:true,simulated:true},token:{ok:false,skipped:true},metadata:{ok:false,skipped:true}},nextAction:'Le Showcase est volontairement simulé. La connexion réelle se teste depuis un backend StoreOps sécurisé.'});
const cell=x=>`<div>${status(x.label,x.tone||'neutral')}</div>`;
const readinessTable=rows=>`<div class="table-wrap"><table class="table"><thead><tr><th>Domaine</th><th>StoreOps</th><th>Connecteur READ</th><th>WRITE</th><th>État pilote</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${esc(r.domain)}</strong></td><td>${cell(r.storeops)}</td><td>${cell(r.read)}</td><td>${cell(r.write)}</td><td class="small">${esc(r.pilot)}</td></tr>`).join('')}</tbody></table></div>`;
const readLine=(label,readModes,key)=>`<div class="focus-meta"><span>${esc(label)}</span><strong>${esc((readModes?.[key]||'simulated').toUpperCase())}</strong></div>`;
export async function renderSystem(){if(!isDirector())return;const d=app.showcase?showcaseDiagnostics():await api('/api/dynamics/diagnostics');const c=d.configuration||{},m=c.mappings||{},readModes=c.readModes||{},ready=integrationReadiness({connected:d.connected,mappings:m,readModes}),counts=readinessCounts(ready);$('#systemContent').innerHTML=`
<div class="grid g2">
 <div class="card"><div class="row"><div><strong>Connexion Dynamics 365</strong><div class="small muted">Diagnostic du connecteur D365. Une connexion technique ne rend jamais automatiquement un domaine métier LIVE.</div></div>${status(d.connected?'Connexion D365 OK':d.mode==='SIMULATED'?'D365 désactivé':'Connexion à configurer',d.connected?'ok':d.mode==='SIMULATED'?'neutral':'danger')}</div><div style="margin-top:14px">${check('1. Configuration',d.checks?.config)}${check('2. Jeton Microsoft Entra',d.checks?.token)}${check('3. Metadata/DataEntities',d.checks?.metadata)}</div>${d.nextAction?`<div class="banner ${d.connected?'ban-info':'ban-danger'}" style="margin-top:12px"><strong>Prochaine action</strong><span>${esc(d.nextAction)}</span></div>`:''}${!app.showcase?'<button id="dynRetest" class="btn brand" style="margin-top:12px">Retester la connexion</button>':''}</div>
 <div class="card"><strong>Domaines READ</strong>${readLine('Article / EAN',readModes,'product')}${readLine('Stock',readModes,'stock')}${readLine('Prix',readModes,'price')}${readLine('Promotions',readModes,'promotion')}${readLine('Réception / PO',readModes,'receiving')}${readLine('Assortiment',readModes,'assortment')}${readLine('Taxonomie',readModes,'taxonomy')}${readLine('Ventes / Business Pulse',readModes,'sales')}<div class="focus-meta"><span>Environment URL</span><strong>${esc(c.baseUrl?.value||'Non configurée')}</strong></div><p class="small muted">Les secrets et jetons ne sont jamais renvoyés au navigateur. Chaque domaine READ doit être mappé et testé séparément.</p></div>
</div>
<div class="card" style="margin-top:14px"><div class="row"><div><strong>Readiness StoreOps / connecteurs</strong><div class="small muted">StoreOps reste indépendant de l’ERP : D365 est le connecteur du POC Franprix, d’autres ERP/PIM/WMS pourront implémenter les mêmes capabilities.</div></div><div class="row"><span class="pill">${counts.liveReads} READ LIVE</span><span class="pill">${counts.readMapping} mapping(s)</span><span class="pill">${counts.writeMapping} write(s) bloqué(s)</span></div></div><div style="margin-top:12px">${readinessTable(ready)}</div><div class="banner ban-info" style="margin-top:12px"><strong>Règle de release</strong><span>Aucun write ERP n’est considéré LIVE sans mapping exact et test de bout en bout. Les parcours StoreOps produisent une trace ou un fichier d’intégration tant que l’écriture directe n’est pas qualifiée.</span></div></div>
<div class="grid g2" style="margin-top:14px">
 <div class="card"><div class="row"><div><strong>Découverte Data Entities</strong><div class="small muted">Metadata F&O permet de valider les noms publics et champs réellement exposés dans l’environnement.</div></div><span class="pill">${d.checks?.metadata?.entityCount??0} entité(s)</span></div>${app.showcase?'<div class="empty" style="margin-top:12px">Disponible après connexion du backend réel.</div>':`<div class="row" style="margin-top:10px"><input id="entitySearch" placeholder="barcode, assortment, category, sales, inventory, PO..." style="flex:1"><button id="entitySearchBtn" class="btn soft">Rechercher</button></div><div id="entityResults"></div>`}</div>
 <div class="card"><strong>Mapping actuel D365</strong><div class="focus-meta"><span>EAN / Barcode</span><strong>${esc(m.barcodeEntity||'À découvrir')}</strong></div><div class="focus-meta"><span>Stock</span><strong>${esc(m.stockEntity||'À découvrir')}</strong></div><div class="focus-meta"><span>Lots</span><strong>${esc(m.stockBatchField||'Détail facultatif non mappé')}</strong></div><div class="focus-meta"><span>Entrepôts source</span><strong>${Object.keys(m.storeSupplyWarehouses||{}).length?`${Object.keys(m.storeSupplyWarehouses).length} magasin(s) mappé(s)`:'À mapper'}</strong></div><div class="focus-meta"><span>Assortiment</span><strong>${esc(m.assortmentEntity||'À découvrir')}</strong></div><div class="focus-meta"><span>Catégories</span><strong>${esc(m.categoryEntity||'À valider')}</strong></div><div class="focus-meta"><span>Affectations produit/catégorie</span><strong>${esc(m.productCategoryAssignmentEntity||'À valider')}</strong></div><div class="focus-meta"><span>Ventes</span><strong>${esc(m.salesEntity||'À valider')}</strong></div><div class="focus-meta"><span>PO</span><strong>${esc(m.purchaseOrderHeaderEntity||'À valider')}</strong></div><div class="small muted">La configuration affichée décrit le connecteur D365 du tenant Franprix ; le kernel StoreOps reste générique.</div></div>
</div>`;
if(app.showcase)return;
$('#dynRetest').onclick=async()=>{const b=$('#dynRetest');b.disabled=true;b.textContent='Test en cours…';try{await api('/api/dynamics/diagnostics?force=1');await renderSystem()}catch(e){b.disabled=false;b.textContent='Retester la connexion';alert(e.message)}};
$('#entitySearchBtn').onclick=async()=>{const host=$('#entityResults');try{const rows=await api(`/api/dynamics/entities?q=${encodeURIComponent($('#entitySearch').value.trim())}`);host.innerHTML=rows.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Entity</th><th>Collection / label</th><th></th></tr></thead><tbody>${rows.slice(0,40).map(x=>{const name=x.PublicCollectionName||x.PublicEntityName||x.Name||x.EntityName||'';return`<tr><td><code>${esc(x.PublicEntityName||x.Name||x.EntityName||name||'Entity')}</code></td><td>${esc(x.PublicCollectionName||x.Label||'')}</td><td>${name?`<button class="btn soft" data-probe-entity="${esc(name)}">Tester</button>`:''}</td></tr>`}).join('')}</tbody></table></div>`:'<div class="empty">Aucune entité trouvée.</div>';host.querySelectorAll('[data-probe-entity]').forEach(b=>b.onclick=async()=>{try{const p=await api(`/api/dynamics/probe?entity=${encodeURIComponent(b.dataset.probeEntity)}&top=1`);alert(p.ok?`OData OK · ${p.entity} · ${p.rowCount} ligne(s) · ${p.latencyMs} ms`:p.message)}catch(e){alert(e.message)}})}catch(e){host.innerHTML=`<div class="banner ban-danger">${esc(e.message)}</div>`}};
}
